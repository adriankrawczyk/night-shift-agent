.DS_Store
*.swp
*.swo
.vscode/
.idea/
node_modules/
.env
.env.*
!.env.example
*.log
runs/
output/
/tmp/

# Project-internal — not for distribution
# (mock-persona dry-runs, scratch notes — not for shipping)
dry-runs/
.scratch/
NOTES.local.md

# Install-time artifacts generated inside the user's installer clone — ignored so
# `git status` in ~/.night-shift-installer stays clean after running the wizard.
start-wizard.command
.claude/
