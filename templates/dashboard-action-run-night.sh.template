#!/usr/bin/env bash
# Manually trigger the night agent (with terminal visible).
# Invoked from the SwiftBar widget action.
#
# Generated by Night Shift Agent installer v{{ installer_version }} at {{ generated_at }}.

set -eu
ROUTINE_DIR="{{ install_dir }}"
cd "$ROUTINE_DIR"

TODAY="$(date +%F)"
echo "→ Triggering manual night agent run..."
echo "→ Log: $ROUTINE_DIR/runs/$TODAY-local.log"
echo ""

FORCE_RUN=1 exec "$ROUTINE_DIR/run.sh"
